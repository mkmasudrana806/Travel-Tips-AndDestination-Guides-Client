import React from "react";

// meta data
export const metadata = {
  title: "Travel Tips And Destination Guides | Notifications",
  description:
    "Travel Tips And Destination Guides, Notifications center you can find your recent activities",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
