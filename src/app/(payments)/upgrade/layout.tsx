import React from "react";

// meta data
export const metadata = {
  title: "Travel Tips And Destination Guides | Upgrade",
  description:
    "Travel Tips And Destination Guides Upgrade page Guide you all about upgrading your account with a subscription",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
