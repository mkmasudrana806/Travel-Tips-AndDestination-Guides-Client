import React from "react";

// meta data
export const metadata = {
  title: "Travel Tips And Destination Guides | Verify Your Account",
  description:
    "Travel Tips And Destination Guides, Verify Your Account gives you clear information about verified account",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
